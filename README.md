# mundoseproyecto1
Se encuentra la aplicacion que se usa para el clima en el proyecto 1
Aplicacion en Python y Flask usada para mostrar el clima.

#Ejecutar localmente

pip install requests

Si muestra el siguiente error "error: externally-managed-environment", se debe realizar lo siguiente:
pip3 install requests --break-system-packages
Si no crear un entorno virtual
python3 -m venv venv
source venv/bin/activate
pip install requests

pip3 install -r requirements.txt

Se deben agregar todas las versiones de requirements.



